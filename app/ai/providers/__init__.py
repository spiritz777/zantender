"""Concrete AI providers.

Import a provider from its module, for example
``from app.ai.providers.gemini import GeminiProvider``. This package avoids
eager SDK imports so the mock remains usable in isolated tests.
"""
